# Step 01 — Rodar CLI doctor

## Comando

```bash
node packages/cli/bin/nmforge.js doctor --cwd <project_root>
```

## Captura

Capture stdout completo. O CLI já formata `[✓] [!] [i]` e devolve resumo.

## Próximo passo

Vá direto para [02-deep-checks.md](02-deep-checks.md). Não bloqueie aqui mesmo se houver warns — o CLI é informativo, não bloqueante por padrão.
